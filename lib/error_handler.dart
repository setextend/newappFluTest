import 'package:flutter/material.dart';

class ErrorHandler {
  static void handleError(BuildContext context, dynamic error) {
    /*final AppProvider = Provider.of<AppProvider>(context, listen: false);

    if (error is NetworkException) {
      AppProvider.setErrorMessage('Нет соединения с интернетом');
    } else if (error is ServerException) {
      AppProvider.setErrorMessage('Ошибка сервера');
    } else {
      AppProvider.setErrorMessage('Произошла непредвиденная ошибка  ');
    }
    // автоматическое скрытие ошибки 3 сек
    Future.delayed(const Duration(seconds: 3), () {
      AppProvider.clearErrorMessage();
    });**/
  }
}
